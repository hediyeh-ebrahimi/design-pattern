package com.tutorial.command.firstSample;


/*
* command is behavioral of design pattern
* converts simple operations to objects that all data the operation needs to don that object has them(data)
*  undo operation
*
* */
public class Main {
    public static void main(String[] args) {

        Editor editor = new Editor();
        editor.init();

    }
}
